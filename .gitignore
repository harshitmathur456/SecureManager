node_modules/
.next/
.data/

# Environment files — never commit real keys
.env*
# Keep the example file tracked (it has no real secrets)
!.env.example

# Logs
*.log

# PDFs — typically large binary assets not needed in source control
*.pdf

# Internal design/brief docs — excluded to keep the repo focused on code
# (UIUX_Brief_*.md, PRD_*.md)
UIUX_Brief_*.md
PRD_*.md
